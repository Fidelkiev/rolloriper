# 📋 CMS Migration Guide - WordPress vs Modern Solutions

## ✅ План зафиксирован!

**Дата фиксации:** 12 февраля 2026  
**Срок реализации:** 4 месяца (16 недель)  
**Старт:** Этап 1 - Design & Inspiration

---

## 🏗️ Текущая структура vs WordPress совместимость

### **Проблема текущей структуры:**
```
rolloripper/
├── index.html          # Статический HTML
├── css/style.css       # Отдельные стили
├── js/script.js        # Отдельные скрипты
└── images/            # Папка с изображениями
```

### **WordPress-совместимая структура:**
```
rolloripper/
├── wp-content/
│   ├── themes/
│   │   └── shoriprofen/
│   │       ├── index.php
│   │       ├── style.css
│   │       ├── functions.php
│   │       └── template-parts/
│   ├── plugins/
│   │   ├── custom-configurator/
│   │   ├── visual-gallery/
│   │   └── smart-home-integration/
│   └── uploads/
│       └── 2026/02/
├── wp-config.php
└── .htaccess
```

---

## 🎯 Рекомендации по CMS для проекта

### **Вариант 1: WordPress + Расширения (Рекомендуемый)**

#### **Преимущества:**
- ✅ Знакомый интерфейс управления
- ✅ Огромная экосистема плагинов
- ✅ Простота миграции
- ✅ Низкие требования к хостингу
- ✅ SEO-дружелюбность

#### **Недостатки:**
- ❌ Ограничения производительности
- ❌ Сложность с кастомными конфигураторами
- ❌ Проблемы с масштабированием

#### **Решение для нашего проекта:**
```php
// Custom Post Types для WordPress
function create_shoriprofen_post_types() {
    // Визуализации
    register_post_type('visualization', [
        'labels' => ['name' => 'Визуализации'],
        'supports' => ['title', 'editor', 'thumbnail', 'custom-fields'],
        'has_archive' => true,
        'public' => true
    ]);
    
    // Конфигурации
    register_post_type('configuration', [
        'labels' => ['name' => 'Конфигурации'],
        'supports' => ['title', 'custom-fields'],
        'public' => false,
        'show_ui' => true
    ]);
    
    // Партнеры
    register_post_type('partner', [
        'labels' => ['name' => 'Партнеры'],
        'supports' => ['title', 'editor', 'thumbnail'],
        'has_archive' => true,
        'public' => true
    ]);
}
add_action('init', 'create_shoriprofen_post_types');
```

---

### **Вариант 2: Headless CMS + Frontend (Продвинутый)**

#### **Технологии:**
- **Strapi** или **Directus** (Backend CMS)
- **Next.js** или **Nuxt.js** (Frontend)
- **PostgreSQL** (База данных)

#### **Преимущества:**
- ✅ Высокая производительность
- ✅ Гибкость в разработке
- ✅ Современный стек
- ✅ Отличное для конфигураторов
- ✅ Легкое масштабирование

#### **Недостатки:**
- ❌ Сложнее в разработке
- ❌ Требует опытной команды
- ❌ Дороже в поддержке

---

### **Вариант 3: Hybrid Solution (Оптимальный)**

#### **Архитектура:**
```
WordPress (Admin) + React Frontend
├── WordPress Backend
│   ├── Управление контентом
│   ├── Пользователи
│   ├── SEO инструменты
│   └── Плагины
└── React Frontend
    ├── Конфигуратор
    ├── Визуализации
    ├── Smart Home
    └── PWA приложение
```

#### **Реализация:**
```javascript
// WordPress REST API + React
const api = {
    visualizations: '/wp-json/wp/v2/visualization',
    products: '/wp-json/wp/v2/product',
    configurations: '/wp-json/shoriprofen/v1/configuration',
    partners: '/wp-json/shoriprofen/v1/partners'
};

// React компоненты
class ShoriProfenApp extends React.Component {
    state = {
        visualizations: [],
        products: [],
        userConfigurations: []
    };
    
    componentDidMount() {
        this.loadData();
    }
    
    async loadData() {
        const visualizations = await fetch(api.visualizations);
        const products = await fetch(api.products);
        
        this.setState({
            visualizations: await visualizations.json(),
            products: await products.json()
        });
    }
}
```

---

## 🚀 План миграции на WordPress

### **Шаг 1: Подготовка структуры (Неделя 1)**
```bash
# Создание WordPress темы
mkdir wp-content/themes/shoriprofen
cd wp-content/themes/shoriprofen

# Копирование текущих файлов
cp ../../../../index.html index.php
cp ../../../../css/style.css style.css
cp ../../../../js/script.js js/script.js
cp -r ../../../../images images/
```

### **Шаг 2: Конвертация в WordPress (Неделя 2)**
```php
// style.css - заголовок темы
/*
Theme Name: Штори ПроФен
Theme URI: https://shoriprofen.ua
Author: Штори ПроФен Team
Description: Современная тема для солнцезащитных систем
Version: 1.0
License: GNU General Public License v2 or later
*/

// functions.php - регистрация скриптов и стилей
function shoriprofen_scripts() {
    wp_enqueue_style('shoriprofen-style', get_stylesheet_uri());
    wp_enqueue_script('shoriprofen-script', 
        get_template_directory_uri() . '/js/script.js', 
        ['jquery'], 
        '1.0.0', 
        true
    );
}
add_action('wp_enqueue_scripts', 'shoriprofen_scripts');
```

### **Шаг 3: Создание кастомных полей (Неделя 3)**
```php
// Использование ACF Pro для визуализаций
if (function_exists('acf_add_local_field_group')) {
    acf_add_local_field_group([
        'key' => 'group_visualization',
        'title' => 'Параметры визуализации',
        'fields' => [
            [
                'key' => 'field_room_type',
                'label' => 'Тип помещения',
                'name' => 'room_type',
                'type' => 'select',
                'choices' => [
                    'bedroom' => 'Спальня',
                    'kitchen' => 'Кухня',
                    'living_room' => 'Гостиная'
                ]
            ],
            [
                'key' => 'field_color_scheme',
                'label' => 'Цветовая схема',
                'name' => 'color_scheme',
                'type' => 'color_picker'
            ],
            [
                'key' => 'field_style',
                'label' => 'Стиль интерьера',
                'name' => 'style',
                'type' => 'select',
                'choices' => [
                    'modern' => 'Модерн',
                    'classic' => 'Классика',
                    'loft' => 'Лофт'
                ]
            ]
        ],
        'location' => [
            [
                [
                    'param' => 'post_type',
                    'operator' => '==',
                    'value' => 'visualization'
                ]
            ]
        ]
    ]);
}
```

---

## 🎯 Рекомендуемый стек для проекта

### **Для быстрого запуска (WordPress):**
```
WordPress 6.5+
├── Тема: Custom ShoriProfen
├── Плагины:
│   ├── Advanced Custom Fields Pro
│   ├── Elementor Pro (для визуального редактора)
│   ├── WPML (многоязычность)
│   ├── Yoast SEO
│   ├── WooCommerce (для интернет-магазина)
│   └── Custom Configurator Plugin
└── Хостинг: Managed WordPress (SiteGround, Kinsta)
```

### **Для максимальной производительности (Hybrid):**
```
Backend: WordPress + REST API
Frontend: Next.js + TypeScript
База: PostgreSQL + Redis
Хостинг: VPS + CDN (Cloudflare)
```

---

## 📊 Сравнение CMS решений

| Критерий | WordPress | Headless (Strapi) | Hybrid |
|----------|------------|-------------------|---------|
| **Скорость разработки** | ⭐⭐⭐⭐⭐ | ⭐⭐⭐ | ⭐⭐⭐⭐ |
| **Производительность** | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ |
| **Масштабируемость** | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ |
| **Стоимость** | ⭐⭐⭐⭐⭐ | ⭐⭐ | ⭐⭐⭐ |
| **SEO** | ⭐⭐⭐⭐⭐ | ⭐⭐⭐ | ⭐⭐⭐⭐ |
| **Конфигураторы** | ⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ |

---

## 🚀 Мой совет

### **Начните с WordPress + Custom Plugin:**
1. **Быстрый запуск** - уже знакомая система
2. **Постепенная миграция** - можно перенести функционал поэтапно
3. **Низкие риски** - проверенное решение
4. **Бюджет** - значительно дешевле

### **План миграции:**
1. **Месяц 1:** Создание WordPress темы и базовой структуры
2. **Месяц 2:** Разработка кастомных плагинов для конфигуратора
3. **Месяц 3:** Интеграция Smart Home и визуализаций
4. **Месяц 4:** Оптимизация и запуск ML-функций

### **Будущее масштабирование:**
- Через год можно перенести frontend на React
- Оставить WordPress как админ-панель
- Постепенно мигрировать на headless архитектуру

**Готовы начать с WordPress или хотите рассмотреть другие варианты?**
